{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${mod_version}",
  "name": "${mod_name}",
  "environment": "*",
  "entrypoints": {
    "client": [
      "dev.isxander.controlify.testharness.FabricEntrypoint"
    ]
  }
}
